# Sessione 2026-08-31

## Cosa è stato fatto

1. **Conversione CLAUDE.md → AGENTS.md**: opencode legge le istruzioni di progetto da `AGENTS.md`, non da `CLAUDE.md`. Contenuto invariato, intestazione adattata, vecchio file rimosso.

2. **Risposte del team DBA ai punti aperti** (raccolte in sessione):
   - **Mount NFS**: ogni target esporta `diag/rdbms` via NFS, montata su `/unipol/logs/database/<technology>/<prod|noprod>/<hostname>/` con struttura ADR sottostante intatta (esempio reale: `axnporadb41` → `np41cdbX/NP41CDBX/trace/alert_NP41CDBX.log`). Layout pensato per future tecnologie (`db2`, `postgres`).
   - **Connection string**: env file da sourcare per ogni `ENVIRONMENT`/`HOSTNAME`/istanza che imposta ORACLE_HOME/ORACLE_SID e la connection string — mai costruita da noi. Il team fornirà anche i comandi per elencare le istanze su un host.
   - **Versioni Oracle**: miste 11g/12c/19c.
   - **Livello di connessione**: sempre CDB, sempre SYSDBA.

3. **Vincolo AIX**: l'utente non sa ancora se in futuro servirà logica nostra direttamente su target AIX → resta come principio di riserva non bloccante (nessuna modifica alle specs, la domanda sospesa della sessione 2026-08-27 si chiude così).

4. **Specs aggiornate** (`docs/specs/architettura.md`):
   - sezione "Convenzioni di ambiente" riscritta con la struttura mount confermata;
   - sezione "Libreria condivisa" riscritta: env file, sempre CDB/SYSDBA, aggiunto candidato `list_instances_on_host.sh` in elenco tool;
   - nuova sezione "Versioni Oracle target": regola di degradazione `unsupported_version` per tool multitenant su 11g.

5. **Nuova spec `docs/specs/contratto-json.md`** (scelta utente: design prima del codice): envelope comune di output (tool, generated_at, environment, hostname, db_name, oracle_version, status, data, error), lista chiusa di codici errore, exit code 0/1/2, regola stdout JSON puro + diagnostica su stderr, esempi di `data` per tool (identify_instance, list_pdbs ok e 11g, scan_alert_log ok e log_not_found).

6. **AGENTS.md**: sezione "Punti aperti" aggiornata — 4 punti risolti registrati, 4 nuovi sottopunti da definire.

## Stato attuale

Nessun codice ancora (solo documentazione). La fase di design del contratto è proposta e in attesa di revisione utente. File: `AGENTS.md`, `oracle-mcp-tools-PROJECT.md` (storico), `docs/specs/{README.md, architettura.md, infrastruttura.md, contratto-json.md}`, `docs/sessions/{README.md, 2026-08-27.md, 2026-08-31.md}`.

## Prossimi passi

1. Utente rivede/approva `docs/specs/contratto-json.md` (eventuali aggiustamenti vanno registrati lì).
2. Chiudere i punti ancora aperti: mapping `ENVIRONMENT` ↔ `prod|noprod`; RAC `DB_NAME` vs `INSTANCE_NAME`; posizione/nome/formato env file; comandi DBA per elencare istanze.
3. Approvato il design → implementazione: `lib/oracle_conn.sh` primo pezzo, poi tool discovery (`list_instances_on_host.sh`, `identify_instance.sh`, `list_pdbs.sh`) per validare il contratto su casi reali (incluso degrado 11g).
4. Successivamente: tool di log analysis su mount NFS, poi FRA/sessioni/PGA.
